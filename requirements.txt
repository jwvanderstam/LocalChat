# Web Framework — FastAPI (ASGI)
fastapi>=0.137.0
uvicorn[standard]>=0.49.0
httpx>=0.28.1          # FastAPI TestClient + async HTTP

# Flask — kept during migration; remove once all routes are ported
Flask==3.1.3
Werkzeug>=3.1.8
flasgger==0.9.7.1
gunicorn==26.0.0

# Caching
redis==8.0.0  # Redis cache backend (optional but recommended)

# Security
Flask-JWT-Extended==4.7.4  # Authentication (Flask; kept during migration)
Flask-Limiter==4.1.1       # Rate limiting (Flask; kept during migration)
Flask-CORS==6.0.4          # CORS support (Flask; kept during migration)
python-jose[cryptography]>=3.5.0  # JWT for FastAPI
slowapi>=0.1.9                     # Rate limiting for FastAPI/Starlette

# Database — PostgreSQL + pgvector
psycopg[binary]>=3.3.4
psycopg-pool>=3.3.1

# Database migrations
alembic>=1.18.4
sqlalchemy>=2.0.50  # required by alembic; postgresql+psycopg dialect for psycopg3

# Validation
pydantic>=2.12.5  # Cannot bump past 2.12.5 until litellm drops its pydantic==2.12.5 exact pin
email-validator==2.3.0

# Document Processing
pypdf>=6.13.0
pdfplumber==0.11.9  # Enhanced PDF extraction with table support
pdfminer.six==20251230  # Pinned: pdfplumber 0.11.9 requires exactly this version
pymupdf4llm>=1.27.2.3    # Layout-aware PDF extraction for column-heavy documents (optional)
langdetect>=1.0.9      # Language detection for multilingual document support
python-docx==1.2.0
python-pptx>=1.0.2
html2text>=2025.4.15          # Confluence HTML→plain-text conversion
reportlab>=4.5.1             # PDF export for conversation download (optional)
kuzu>=0.11.3               # Graph store backend (optional; set GRAPH_BACKEND=kuzu to activate)
openpyxl>=3.1.5         # Excel (.xlsx) ingestion
litellm>=1.83.7    # Cloud model fallback — optional, only needed when CLOUD_FALLBACK_ENABLED=true; >=1.83.7 fixes auth bypass CVEs
h11>=0.16.0        # Pinned directly: 0.16.0 fixes CVE-2025-43859 (HTTP request smuggling)
spacy>=3.8.14      # GraphRAG entity extraction — optional; also run: python -m spacy download en_core_web_sm

# Encryption — OAuth token at-rest encryption
cryptography>=48.0.0
# Cross-encoder reranker — enabled by default (RERANKER_ENABLED=true)
sentence-transformers>=5.5.1
# Optional: Microsoft Graph API auth helper
# msal>=1.28

# HTTP Requests
requests==2.34.2
ddgs>=9.14.4

# Numerical processing for vector operations
numpy>=2.4.6

# Multipart form handling
python-multipart==0.0.32

# Markdown rendering
markdown==3.10.2

# Environment configuration
python-dotenv>=1.2.2

# Testing
pytest==9.0.3
pytest-cov==7.1.0
pytest-mock==3.15.1
faker==40.21.0
responses==0.26.1
freezegun==1.5.5
coverage==7.14.1
