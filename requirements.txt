# Flask and Web Framework
Flask==3.1.3
Werkzeug>=3.1.6
flasgger==0.9.7.1  # OpenAPI/Swagger documentation
gunicorn==25.3.0   # Production WSGI server

# Caching
redis==7.4.0  # Redis cache backend (optional but recommended)

# Security
Flask-JWT-Extended==4.7.1  # Authentication
Flask-Limiter==4.1.1       # Rate limiting
Flask-CORS==6.0.2          # CORS support

# Database — PostgreSQL + pgvector
psycopg[binary]>=3.3.3
psycopg-pool>=3.3.0

# Validation
pydantic==2.13.2
email-validator==2.3.0

# Document Processing
pypdf>=6.10.2
pdfplumber==0.11.9  # Enhanced PDF extraction with table support
pdfminer.six==20251230  # Pinned: pdfplumber 0.11.9 requires exactly this version
python-docx==1.2.0
python-pptx>=1.0.2
litellm>=1.72.6    # Cloud model fallback — optional, only needed when CLOUD_FALLBACK_ENABLED=true; >=1.72.6 fixes auth bypass CVEs
h11>=0.16.0        # Pinned directly: 0.16.0 fixes CVE-2025-43859 (HTTP request smuggling)
spacy>=3.7.0       # GraphRAG entity extraction — optional; also run: python -m spacy download en_core_web_sm

# Encryption — OAuth token at-rest encryption
cryptography>=44.0.0
# Optional: Fine-tuned cross-encoder reranker — enable with RERANKER_ENABLED=true
# sentence-transformers>=3.0
# Optional: Microsoft Graph API auth helper
# msal>=1.28

# HTTP Requests
requests==2.33.1
ddgs>=9.0.0

# Numerical processing for vector operations
numpy>=2.4.2

# Multipart form handling
python-multipart==0.0.26

# Markdown rendering
markdown==3.10.2

# Environment configuration
python-dotenv==1.2.2

# Testing
pytest==9.0.3
pytest-cov==7.1.0
pytest-mock==3.15.1
faker==40.15.0
responses==0.26.0
freezegun==1.5.5
coverage==7.13.5
