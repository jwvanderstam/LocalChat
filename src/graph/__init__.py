"""
GraphRAG Package
================

Entity extraction and graph-based query expansion for LocalChat's
knowledge graph layer (Feature 2.3).

spaCy is an optional dependency — if it is not installed or the model is
missing, all graph operations degrade gracefully to no-ops.
"""
